export class GameInstance {
    public start = (): void => {
        // const player = new Player()
    }

    public getRace = (choice) => {
        console.log(choice)
    }

    public getCharacter = (choice) => {
    }
}